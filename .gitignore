node_modules/
.env
.env.*
*.log
.DS_Store

# Content and build output — generated by the synthesis pipeline.
# The content/ directory holds auto-generated insight papers (may contain
# private data). The dist/ directory holds compiled HTML output.
# Both are produced at runtime and should not be committed.
content/
dist/
